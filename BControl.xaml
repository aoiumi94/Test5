<UserControl
    x:Class="Test5.BControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Test5"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <Grid>
        <StackPanel Orientation="Vertical">
            <!--<local:ATextBox Text="{Binding Title}" Width="200"/>
            <local:ATextBox Text="{Binding Name}" Width="200"/>-->
            <TextBox Text="{Binding Title}" Width="200"/>
            <TextBox Text="{Binding Name}" Width="200"/>
        </StackPanel>

    </Grid>
</UserControl>
